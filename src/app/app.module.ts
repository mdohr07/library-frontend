import { NgModule } from '@angular/core';
import { AppRoutingModule } from './app-routing.module';
import { BrowserModule } from '@angular/platform-browser';
import { App } from './app';

@NgModule({
  declarations: [
    // Declare your components here, e.g., AppComponent
  ],
  imports: [
    BrowserModule,
    AppRoutingModule,
  ],
})
export class AppModule { }
